1. Enter api_id and api_hash to config.ini (get it: my.telegram.org)
2. Install all necessary packages with command "pip install -r requirements.txt"
3. Open config.py and enter your telegram id as admin
4. Run the client-bot (main.py)
